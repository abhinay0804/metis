# Archived reversible masking code

This folder contains the code that previously supported reversible masking (storing and retrieving originals).

Files:
- storage_reversible.py: EncryptedLocalStore (encrypted storage of originals)
- app_reversible_snippets.py: previous FastAPI route snippets for processing with reversible flag and fetching originals

To restore in the future, move these back into server/storage.py and server/app.py respectively and re-enable the UI buttons.
